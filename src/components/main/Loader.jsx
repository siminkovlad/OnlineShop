import React from 'react';

import { LoaderStyled, DivStyled } from '@styles/styledComponents/styledLoader';

export const Loader = () => (
    <LoaderStyled>
        <DivStyled />
    </LoaderStyled>
);
